name: Update Iran Proxies

on:
  schedule:
    - cron: '0 */7 * * *'  # هر 6 ساعت یکبار
  workflow_dispatch:
    inputs:
      manual_trigger:
        description: 'Trigger manually'
        required: false
        default: 'false'

permissions:
  contents: write

jobs:
  update-proxies:
    runs-on: ubuntu-latest
    timeout-minutes: 360  # افزایش به 2 ساعت
    
    steps:
    - name: 📥 Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        token: ${{ secrets.GITHUB_TOKEN }}
    
    - name: 🐍 Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
        cache: 'pip'
    
    - name: 📦 Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install beautifulsoup4 lxml
    
    - name: 💾 Commit and push changes
      run: |
        git config --global user.name "github-actions[bot]"
        git config --global user.email "github-actions[bot]@users.noreply.github.com"
        
        # اول تغییرات جدید را بگیر و merge کن
        echo "📥 دریافت آخرین تغییرات از main..."
        git fetch origin
        git merge origin/main --no-edit || git merge --abort
        
        # اسکریپت را اجرا کن
        echo "🚀 اجرای اسکریپت به‌روزرسانی..."
        python scripts/update.py
        
        # فایل‌های تغییر کرده را اضافه کن
        git add output/config.yaml output/logs/latest.log 2>/dev/null || true
        
        # اگر تغییری وجود دارد commit کن
        if ! git diff --cached --quiet; then
          COMMIT_DATE=$(date -u '+%Y-%m-%d %H:%M UTC')
          git commit -m "🤖 Update proxy list - $COMMIT_DATE"
          
          # دوباره تغییرات را بگیر قبل از push
          git pull origin main --rebase --autostash
          
          # Push تغییرات با force اگر لازم باشد
          git push origin HEAD:main || git push origin HEAD:main --force
          echo "✅ Changes pushed successfully"
          
          # نمایش خلاصه
          echo "📋 خلاصه اجرا:"
          tail -5 output/logs/latest.log 2>/dev/null || echo "⚠️ فایل لاگ پیدا نشد"
        else
          echo "⚠️ No changes to commit"
        fi
