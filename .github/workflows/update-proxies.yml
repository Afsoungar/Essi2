name: Update Iran Proxies

on:
  schedule:
    - cron: '0 */7 * * *'  # هر 6 ساعت یکبار
  workflow_dispatch:
    inputs:
      manual_trigger:
        description: 'Trigger manually'
        required: false
        default: 'false'

permissions:
  contents: write

jobs:
  update-proxies:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    
    steps:
    - name: 📥 Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        token: ${{ secrets.GITHUB_TOKEN }}
    
    - name: 🐍 Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
        cache: 'pip'
    
    - name: 📦 Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install beautifulsoup4 lxml
    
    - name: 🚀 Run update script
      run: |
        echo "Starting proxy update process..."
        python scripts/update.py
        echo "Update script completed!"
    
    - name: 💾 Commit and push changes
      run: |
        git config --global user.name "github-actions[bot]"
        git config --global user.email "github-actions[bot]@users.noreply.github.com"
        
        # حذف فایل قدیمی config اگر وجود دارد
        rm -f output/config.yaml
        
        # از آخرین تغییرات آپدیت شو
        git fetch origin
        git reset --hard origin/main
        
        # دوباره اسکریپت را اجرا کن (با اطلاعات جدید)
        python scripts/update.py
        
        # فقط فایل config را اضافه کن
        git add output/config.yaml
        
        # اگر تغییری وجود دارد commit کن
        if ! git diff --cached --quiet; then
          COMMIT_DATE=$(date -u '+%Y-%m-%d %H:%M UTC')
          git commit -m "🤖 Update proxy list - $COMMIT_DATE"
          
          # Push با force (مطمئن شو کار می‌کند)
          git push origin HEAD:main
          echo "✅ Changes pushed successfully"
        else
          echo "⚠️ No changes to commit"
        fi
